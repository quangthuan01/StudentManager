<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asm_ps10375_nguyenquangthuan">

    <application
        android:allowBackup="true"
        android:icon="@drawable/presentation"
        android:label="@string/app_name"
        android:roundIcon="@drawable/presentation"
        android:supportsRtl="true"
        android:theme="@style/Theme.ASM_PS10375_NguyenQuangThuan">
        <activity android:name=".Login.RegisterActivity" />
        <activity android:name=".Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.ActivityContact" />
        <activity android:name=".Activity.ActivityManager" />
        <activity android:name=".Activity.ActivityList" />
        <activity android:name=".Activity.MainActivity"></activity>
    </application>

</manifest>